<UserControl x:Class="Trojan.UI.Views.Components.AlbumItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             MinWidth="160"
             MinHeight="220">

    <Grid Margin="10">

        <Border CornerRadius="16"
                Background="White"
                Padding="10"
                SnapsToDevicePixels="True">


            <Grid Cursor="Hand">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- TITLE -->
                <TextBlock Grid.Row="0"
                           x:Name="TitleText"
                           FontSize="16"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Margin="0,0,0,10"/>

                <!-- IMAGE -->
                <Border Grid.Row="1"
                        CornerRadius="12"
                        Background="#F3F3F3"
                        ClipToBounds="True"
                        Margin="0,0,0,10">

                    <Image x:Name="AlbumImageControl"
                           Stretch="UniformToFill"/>

                </Border>

                <!-- COUNT -->
                <TextBlock Grid.Row="2"
                           x:Name="CountText"
                           FontSize="12"
                           Opacity="0.7"
                           HorizontalAlignment="Center"
                           Margin="0,4,0,2"/>

            </Grid>

        </Border>

    </Grid>

</UserControl>